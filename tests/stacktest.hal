load "stack";
fun main()
    array nums = [10, 20, 30, 40, 50];
    var length = len(nums);

    var removed = pop(nums, length);
    length = length - 1;
    print("removed element: %i\n", removed);

     var i = 0;
    while i < length
        print("%i\n", nums[i]);
        
        i = i + 1;
    elihw


    push(nums, length, 99);
    length = length + 1;

    print("After push:\n");
    var j = 0;
    while j < length
        print("%i\n", nums[j]);
        j = j + 1;
    elihw

    return 0;
nuf