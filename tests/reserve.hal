fun main()
    var p = reserve(4 * 10);    
    deref(p) = 42;            
    print("%i\n", deref(p));

    var q = p + 1;           
    deref(q) = 99;
    print("%i\n", deref(q));

    free(p);                  
    return 0;
nuf
